apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: haru-han-jan
  labels:
    name: haru-han-jan
spec:
  rules:
  #- host: v1.echo.127.0.0.1.sslip.io
   - http:
      paths:
      - pathType: Prefix
        path: "/api/alcohol"
        backend:
          service:
            name: alcohol-service
            port: 
              number: 8080
